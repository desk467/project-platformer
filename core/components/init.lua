local components = {
    Active      = require('core.components.active'),
    Drawable    = require('core.components.drawable'),
    Writable    = require('core.components.writable'),
    Position    = require('core.components.position'),
    Velocity    = require('core.components.velocity'),
}

return components
