local components = {
    Active      = require('core.ecs.components.active'),
    Drawable    = require('core.ecs.components.drawable'),
    Writable    = require('core.ecs.components.writable'),
    Position    = require('core.ecs.components.position'),
    Velocity    = require('core.ecs.components.velocity'),
}

return components
